# 社媒内容需求分析方法

# 0. 调研背景

Atoms 用户在平台上构建应用后，普遍面临「应用做出来了，但没人用」的冷启动困境。社交媒体是 SMB 用户最主要的获客渠道，但内容创作能力门槛高。本调研目的是验证：用户是否对平台内置「**为我的应用生成社媒推广内容**」的 Agent 能力存在真实且规模化的需求。

| 编号 | 问题 | 期望输出形式 |
| --- | --- | --- |
| Q1 | Atoms 用户对自建应用的冷启动 + 长期社媒运营是否有明确需求 | 是/否 + 命中用户数 + 显性占比 |
| Q2 | 若有，需求规模与紧迫度如何 | TAM 上下限 + 紧迫度信号统计 |
| Q3 | 需求可聚类为哪些细分方向 | 4-8 个子类 + 每子类用户画像 + 高频诉求 |

## 1. 分析目标与核心判断问题

本分析的目标不是简单统计“出现社媒关键词的 prompt 数量”，而是判断 Atoms builder 是否存在真实、规模化、可产品化的社媒内容生产与增长引流需求。

核心问题包括：

1. 需求是否真实存在，规模有多大。
2. 需求是否稳定存在，还是偶发噪音。
3. 用户是在推广自己构建的应用，还是在构建营销工具。
4. 用户最需要哪些平台、内容形式和增长动作。
5. 需求出现在 builder 生命周期的哪个节点。
6. 这些需求是否能沉淀为标准化 Agent 工作流。

## 2. 数据采集方法与基础口径

### 2.1 数据源

| **项目** | **口径** |
| --- | --- |
| 数据库 | `atoms_dwm` |
| 数据表 | `atoms_dwm.dwm_user_prompt_turn_message_detail` |
| 时间窗 | `2026-02-01 <= message_date < 2026-06-25` |
| 文本字段 | `content_preview` |
| 主键锚点 | `message_id` |
| 会话维度 | `chat_id` |
| 用户维度 | `user_id` |

本次导出使用 `content_preview`，不是完整 `content`。原因是命中样本中存在 HTML、CSS、JS、附件结构、网页源码等超长输入，最大单条超过 800 万字符，直接拉取完整 `content` 会显著增加导出和处理成本。

后续如需做高价值样本深描，应按 `message_id` 小批量回补完整 `content`。

### 2.2 严格用户 prompt 过滤口径

只统计真实用户 prompt，不统计 agent 回复、系统消息或中间过程消息。

```sql
is_user_prompt = TRUE
AND role = 'User'
AND turn_boundary_type = 'user_prompt'
```

### 2.3 同期全量基准

| **指标** | **数量** |
| --- | --- |
| 全量 prompt 数 | 1,873,023 |
| 全量 user 数 | 332,272 |
| 全量 chat 数 | 486,162 |

这些全量基准用于计算命中 prompt/user/chat 的渗透率。

## 3. 文件产出与长度过滤口径

### 3.1 主文件

| **文件** | **作用** |
| --- | --- |
| `raw/20260624_b1_prompt_keyword_hits_preview_core.csv` | 原始 preview 命中池，用于回溯、校验、完整候选保留 |
| `processed/20260624_b1_prompt_keyword_hits_preview_core_len_le_100k.csv` | 移除 `content_len > 100000` 的极端长 prompt，用于规模统计和月度趋势 |
| `processed/20260624_b1_prompt_keyword_hits_preview_core_len_le_10k.csv` | 移除 `content_len > 10000` 的长文本 prompt，用于语义分析、样本审阅、打标 |

### 3.2 长度过滤结果

| **口径** | **保留 prompt 数** | **移除 prompt 数** | **建议用途** |
| --- | --- | --- | --- |
| 原始 preview_core | 43,038 | 0 | 原始候选池 |
| `content_len <= 100000` | 42,811 | 227 | 规模统计、趋势分析 |
| `content_len <= 10000` | 40,271 | 2,767 | 一轮打标、语义洞察、二轮候选 |

### 3.3 不同口径下的规模

| **口径** | **prompt 数** | **user 数** | **chat 数** | **prompt 占比** | **user 占比** | **chat 占比** |
| --- | --- | --- | --- | --- | --- | --- |
| 原始 preview 命中池 | 43,038 | 26,942 | 32,360 | 2.30% | 8.11% | 6.66% |
| `content_len <= 100000` | 42,811 | 26,785 | 32,173 | 2.29% | 8.06% | 6.62% |
| `content_len <= 10000` | 40,271 | 25,206 | 30,259 | 2.15% | 7.59% | 6.22% |

结论：即便采用较严格的 `content_len <= 10000` 洞察口径，仍覆盖 7.59% 的用户和 6.22% 的 chat，说明社媒营销相关需求不是极小众噪音。

## 4. 关键词命中体系设计

### 4.1 设计原则

关键词命中体系用于“召回候选”，不直接等同于真实需求。它的设计目标是提高召回率，同时通过后续一轮、二轮打标校正噪音。

核心原则：

1. 平台词单独出现不等于营销需求。
2. 营销动作词、内容生产词、业务对象词共同出现时，需求置信度更高。
3. 技术集成、社交登录、分享按钮、页脚链接、社媒 icon、UI 风格参考要单独识别为噪音或低相关。
4. 长文本、源码、PRD、日志中可能偶然出现大量平台词，需要长度过滤和语义打标双重校正。

### 4.2 关键词模块

| **模块** | **目的** | **典型信号** |
| --- | --- | --- |
| 平台词 | 识别社媒渠道与分发平台 | Instagram、TikTok、YouTube、Facebook、LinkedIn、X、Reddit、小红书、抖音、Pinterest、WhatsApp、Threads |
| 内容形式词 | 识别内容生产需求 | caption、headline、title、hashtag、video script、short video、carousel、thread、content calendar、文案、标题、脚本、封面、图文、选题、口播 |
| 增长动作词 | 识别推广、获客、增长意图 | cold start、no users、get users、find users、get traffic、user acquisition、GTM、growth strategy、launch strategy、推广、引流、获客、冷启动、投放、找用户 |
| 自有应用词 | 判断是否可能是 builder 自己的应用 | my app、our app、my website、my product、my SaaS、I built、I created、我的应用、我的产品、我的网站、我的工具、我开发的 |
| App 推广词 | 识别直接推广意图 | promote my app、launch my app、market my product、help me launch、推广我的、宣传我的、帮我推广 |
| 营销工具词 | 识别“构建营销工具”场景 | marketing platform、social media management、campaign dashboard、ad manager、SEO agent、content agent、营销平台、营销工具、社媒工具、排程工具、发帖工具、达人插件 |
| 技术集成词 | 识别平台技术接入与噪音 | OAuth、webhook、API、SDK、social login、share button、callback、授权、分享到、connect and sync data |
| 技术噪音词 | 识别报错、日志、源码类误命中 | error、exception、debug、diagnostics、console、stack trace、failed、fix、bug、RLS、Supabase、SQL、JavaScript、TypeScript、bash、日志、报错、修复 |
| 弱相关/无关词 | 识别 UI、页脚、链接、风格参考 | social media logo、social media link、footer、follow us、share icon、Instagram style、小红书风格、perfiles de redes sociales |

### 4.3 平台识别标准

| **标签** | **识别信号** |
| --- | --- |
| `xiaohongshu` | 小红书、xiaohongshu、rednote |
| `douyin` | 抖音、douyin |
| `tiktok` | tiktok |
| `instagram` | instagram、IG |
| `youtube` | youtube |
| `linkedin` | linkedin |
| `facebook` | facebook |
| `reddit` | reddit |
| `threads` | threads |
| `x` | twitter、x.com、推特 |
| `pinterest` | pinterest |
| `whatsapp` | whatsapp |

平台词用于辅助判断，不单独构成强需求。若仅出现平台链接、登录按钮、分享按钮或页脚图标，通常标为低相关或无关。

### 4.4 内容形式识别标准

| **标签** | **识别信号** |
| --- | --- |
| `caption` | caption、captions、配文、文案 |
| `title` | headline、headlines、title、titles、标题 |
| `hashtag` | hashtag、hashtags、话题标签、标签 |
| `short_video_script` | video script、short video、reel script、tiktok script、短视频脚本、口播稿、分镜、脚本 |
| `image_post` | 图文、carousel、poster、海报、封面 |
| `content_calendar` | content calendar、content plan、posting plan、发布计划、选题、日历 |
| `thread` | thread、threads、长帖 |
| `email_copy` | email copy、邮件文案、newsletter |
| `ad_copy` | ad copy、广告文案、广告素材 |

## 5. 命中层级与排除规则

### 5.1 数据库粗筛与本地规则过滤

数据库侧先通过核心 LIKE 条件粗筛，得到 47,743 条候选。本地按 v2 分层规则进一步过滤，保留 43,038 条。

| **类型** | **数量** |
| --- | --- |
| 数据库侧核心 LIKE 粗筛 | 47,743 |
| 本地 v2 规则保留 | 43,038 |
| 技术集成类 `EXCLUDED_INTEGRATION` 过滤 | 2,836 |
| 命中粗筛但不满足 v2 分层规则 | 1,869 |

### 5.2 hit_level 分层

| **hit_level** | **定义** | **prompt 数** | **user 数** | **chat 数** |
| --- | --- | --- | --- | --- |
| `HARD` | 平台词、营销动作词、内容形式词、业务对象词等强信号组合命中 | 42,616 | 26,726 | 32,058 |
| `SOFT` | 有社媒或营销语境，但信号组合较弱 | 195 | 153 | 178 |
| `WEAK` | 只有平台、链接、社媒元素或风格词，保留用于抽样校验 | 227 | 174 | 192 |

注意：`HARD` 不等于强需求。它只代表关键词组合强，仍可能包含 API、登录、分享按钮、网页源码、UI 风格等噪音。

## 6. 一轮 prompt 打标体系

### 6.1 输入与目标

| **项目** | **内容** |
| --- | --- |
| 输入文件 | `processed/20260624_b1_prompt_keyword_hits_preview_core_len_le_10k.csv` |
| 输出文件 | `processed/20260624_b1_prompt_keyword_hits_preview_core_len_le_10k_labeled.csv` |
| 样本量 | 40,271 条 |
| 分析单元 | 单条 prompt |
| 目标 | 对关键词命中池做轻量语义校正，筛出高/中相关核心池 |

第一轮不补上下文，只看单条 prompt。因此它适合判断“这条 prompt 是否相关、主要动作是什么、出现了哪些平台和内容形式”，不适合最终判断“是否在推广自己构建的应用”。

### 6.2 一轮输出字段

| **字段** | **取值** | **说明** |
| --- | --- | --- |
| `relevance` | high、medium、low、irrelevant | 单条 prompt 与社媒营销或增长需求的相关性 |
| `primary_intent` | app_promotion、content_generation、growth_strategy、marketing_tool_builder、platform_integration、irrelevant、unclear | 单条 prompt 的主意图 |
| `is_promoting_own_app` | true、false、unclear | 单条 prompt 是否能看出用户在推广自己构建的应用 |
| `content_formats` | 多标签 | 抽取 title、caption、hashtag、image_post 等内容形式 |
| `urgency` | high、medium、low、unclear | 单条 prompt 的紧迫度 |
| `evidence` | 文本片段 | 支持判断的短证据 |

### 6.3 一轮相关性规则

| **标签** | **判定标准** |
| --- | --- |
| `high` | 明确出现内容生成、增长策略、推广应用等营销目标，并伴随平台、内容形式或增长动作信号 |
| `medium` | 与社媒营销相关，但对象或目标不完整；或营销工具构建类需求 |
| `low` | 只有平台词、技术集成、社媒元素、风格参考等弱相关信号 |
| `irrelevant` | 技术报错、源码、上传失败、登录/API/授权、页脚链接、logo、mockup 等无实际社媒营销目标 |

一轮相关性判断的关键逻辑：

1. 若出现技术噪音词，且没有营销工具、App 推广或增长语境，优先 `irrelevant`。
2. 若出现技术集成词和平台词，但没有内容、增长、推广目标，通常 `low`。
3. 若出现无关词，如 social media link、footer、share icon、Instagram style、小红书风格，且没有内容或增长动作，通常 `irrelevant` 或 `low`。
4. 若出现 App 推广词，或自有应用词与内容/增长动作共同出现，通常 `high`。
5. 若出现内容生成词并伴随社媒上下文，通常 `high`。
6. 若出现营销工具词，通常 `medium` 或 `marketing_tool_builder`，需要与 builder 自身应用增长分开。

### 6.4 一轮主意图规则

主意图按单选处理，核心优先级如下：

| **主意图** | **判定标准** |
| --- | --- |
| `marketing_tool_builder` | 出现营销平台、社媒工具、广告管理、排程工具、达人插件等构建营销工具信号 |
| `platform_integration` | 出现 OAuth、API、SDK、social login、share button、webhook 等技术集成信号，且没有明确营销目标 |
| `app_promotion` | 出现 promote my app、launch my app、推广我的、帮我推广等直接推广信号 |
| `growth_strategy` | 出现 cold start、no users、get users、traffic、GTM、获客、引流、投放、找用户等增长策略信号 |
| `content_generation` | 出现 caption、title、hashtag、script、carousel、文案、标题、脚本、图文等内容生产信号，并有社媒上下文 |
| `unclear` | 相关但信息不足 |
| `irrelevant` | 无关或误命中 |

### 6.5 一轮紧迫度规则

| **标签** | **信号** |
| --- | --- |
| `high` | asap、urgent、immediately、right now、today、马上、尽快、立即、没人用、no users、没有流量、冷启动 |
| `medium` | launch、go live、this week、soon、准备上线、上线、发布、投放 |
| `low` | 无明显时效或痛点，或无关样本 |
| `unclear` | 内容/增长相关，但单条 prompt 无法判断紧迫度 |

### 6.6 一轮量化结果

| **指标** | **数量** | **占比** |
| --- | --- | --- |
| 一轮输入样本 | 40,271 | 100.00% |
| high relevance | 4,712 | 11.70% |
| medium relevance | 958 | 2.40% |
| high + medium 核心池 | 5,670 | 14.10% |
| low relevance | 25,976 | 64.50% |
| irrelevant | 8,625 | 21.40% |

一轮结论：关键词命中池中高/中相关样本占 14.1%，说明原始关键词命中不能直接作为真实需求规模，必须经过语义校正。

## 7. 二轮上下文打标体系

### 7.1 输入与目标

| **项目** | **内容** |
| --- | --- |
| 输入文件 | `processed/20260624_b1_prompt_keyword_hits_preview_core_len_le_10k_labeled_context_user_2.csv` |
| 输出文件 | `processed/20260624_b1_prompt_keyword_hits_preview_core_len_le_10k_labeled_context_user_2_round2.csv` |
| 样本量 | 5,670 条 |
| 分析单元 | context case |
| 输入范围 | 一轮 high/medium 核心池 |

二轮不再只回答“这条 prompt 是否命中社媒关键词”，而是回答更接近产品立项的问题：

1. 这个需求是否真实属于社媒内容营销或增长引流。
2. 用户到底在推广什么。
3. 是否是 builder 自己构建的应用。
4. 需求处于 builder 生命周期哪个阶段。
5. 需求强度和紧迫度是否足够高。
6. 能否标准化为社媒营销 Agent 工作流。

### 7.2 上下文补充口径

以命中 prompt 为锚点，在同一个 `chat_id` 内补充用户上下文：

| **字段** | **说明** |
| --- | --- |
| `first_user_prompt_preview` | 该 chat 的首条用户 prompt，用于判断用户最初构建什么 |
| `prev2_user_prompt_preview` | 命中前第 2 条用户 prompt |
| `prev1_user_prompt_preview` | 命中前第 1 条用户 prompt |
| `prompt_text_preview` 或 `extracted_text` | 命中 prompt 本身 |
| `next1_user_prompt_preview` | 命中后第 1 条用户 prompt |
| `next2_user_prompt_preview` | 命中后第 2 条用户 prompt |

上下文证据优先级：

```
chat_first_prompt > prev_user_prompts > hit_prompt > next_user_prompts
```

原因：

1. 首条 prompt 通常最能说明用户要构建什么。
2. 前序 prompt 通常能补足应用背景、业务类型和修改原因。
3. 命中 prompt 最能说明当前营销动作。
4. 后续 prompt 用于判断用户是否继续围绕营销、内容、获客推进。

### 7.3 二轮输出字段

| **字段** | **取值或说明** |
| --- | --- |
| `context_relevance` | high、medium、low、irrelevant |
| `context_primary_intent` | app_promotion、content_generation、growth_strategy、marketing_tool_builder、platform_integration、irrelevant、unclear |
| `is_promoting_own_app_context` | true、false、unclear |
| `promoted_object` | own_built_app、own_business、client_business、marketing_tool、social_account、content_asset、unknown |
| `builder_stage` | idea、building、pre_launch、launch、post_launch、operating、unclear |
| `business_type` | saas_ai_tool、ecommerce、course、local_service、creator、game、landing_page、marketplace、agency、nonprofit、unknown |
| `platforms_context` | 结合上下文抽取的平台 |
| `content_formats_context` | 结合上下文抽取的内容形式 |
| `pain_points` | cold_start、no_users、low_traffic、low_conversion 等 |
| `urgency_score` | 0-100 |
| `context_urgency` | high、medium、low、unclear |
| `demand_strength_score` | 0-100 |
| `demand_strength` | strong、medium、weak、unclear |
| `agent_fit_score` | 0-100 |
| `agent_opportunity` | social_launch_pack、platform_specific_copy、growth_diagnosis 等 |
| `segment` | builder_app_growth、content_creation、marketing_tool_builder、client_marketing、own_business_growth、noise |
| `confidence` | 0.35-0.96 |
| `context_evidence` | first、prev、hit、next 的短证据 |
| `needs_more_context` | true、false |
| `exclude_reason` | 排除原因 |

## 8. 二轮核心分类规则

### 8.1 `context_relevance`

| **标签** | **标准** |
| --- | --- |
| `high` | 上下文明确出现推广、获客、流量、发布、社媒内容、平台投放、达人、增长策略等需求，且能判断推广对象或营销目标 |
| `medium` | 与社媒内容或增长相关，但推广对象、目标受众、平台或业务背景不完整 |
| `low` | 只有社媒链接、社媒 icon、分享按钮、平台风格、泛内容编辑等弱相关信号 |
| `irrelevant` | 技术报错、登录/API/授权、UI 修改、网页源码、平台链接维护、纯品牌展示等，不构成社媒营销需求 |

### 8.2 `context_primary_intent`

| **标签** | **标准** |
| --- | --- |
| `app_promotion` | 用户想推广自己构建的 app、网站、SaaS、工具、游戏、落地页或产品 |
| `content_generation` | 用户主要要求生成社媒文案、标题、caption、hashtag、短视频脚本、图文卡片、内容日历等 |
| `growth_strategy` | 用户主要在问冷启动、获客、找用户、投放、渠道策略、转化、增长路径 |
| `marketing_tool_builder` | 用户正在构建营销平台、内容工具、发帖排程工具、广告管理工具、达人搜索插件等 |
| `platform_integration` | 用户主要在做社媒登录、OAuth、API、Webhook、分享按钮、账号连接、自动发布接口等技术集成 |
| `irrelevant` | 上下文确认不是社媒营销需求 |
| `unclear` | 上下文仍不足以判断主意图 |

主意图判定优先级：

```
marketing_tool_builder 或 marketing_tool 优先识别
platform_integration 在没有营销目标时单独识别
own_built_app + 推广/增长/内容动作 => app_promotion
增长词强于普通内容生成 => growth_strategy
内容生成词 + 社媒上下文 => content_generation
```

### 8.3 `promoted_object`

| **标签** | **标准** |
| --- | --- |
| `own_built_app` | 上下文明确用户正在 Atoms 中构建 app、网站、SaaS、工具、游戏、落地页，并要求推广、发布、获客或生成内容 |
| `own_business` | 用户在推广自己的业务、店铺、课程、咨询服务、电商品牌、个人品牌，但不一定是 Atoms 构建的 app |
| `client_business` | 用户像 agency、代运营、营销服务商一样，为客户品牌或客户业务做内容和增长 |
| `marketing_tool` | 推广对象不是业务本身，而是用户要构建的营销工具、内容工具、广告工具、达人搜索工具 |
| `social_account` | 目标主要是运营一个社媒账号或创作者账号 |
| `content_asset` | 目标只是包装 logo、图片、视频、文章、博客、作品集等内容资产 |
| `unknown` | 上下文不足，无法判断推广对象 |

`is_promoting_own_app_context` 规则：

| **取值** | **标准** |
| --- | --- |
| true | `promoted_object = own_built_app` |
| false | 明确是客户业务、营销工具、社媒账号、内容资产、技术集成或无关场景 |
| unclear | 有内容或增长动作，但推广对象 unknown，且上下文相关性为 high/medium |

### 8.4 `builder_stage`

| **标签** | **标准** |
| --- | --- |
| `idea` | 构思产品、选品、定位、商业模式、目标用户、验证想法 |
| `building` | 正在开发功能、页面、数据库、UI、业务流程，未出现明显发布动作 |
| `pre_launch` | 准备上线、即将发布、预热、waitlist、coming soon |
| `launch` | 明确发布、上线、投放、首发、launch plan |
| `post_launch` | 已发布后出现没人用、没流量、无注册、转化低、需要找用户 |
| `operating` | 持续运营内容、账号、广告、社区、CRM、newsletter、复购和留存 |
| `unclear` | 上下文不足，无法判断阶段 |

### 8.5 `business_type`

| **标签** | **标准** |
| --- | --- |
| `saas_ai_tool` | SaaS、AI、agent、dashboard、productivity、tool、platform、automation、软件、工具、平台、自动化、智能体 |
| `ecommerce` | ecommerce、Shopify、WooCommerce、store、shop、DTC、dropship、电商、店铺、带货、商品 |
| `course` | course、coaching、training、education、课程、培训、教育、知识付费 |
| `local_service` | restaurant、salon、clinic、dental、real estate、repair、餐厅、美容、门店、诊所、房产、维修 |
| `game` | game、gaming、小游戏、游戏 |
| `marketplace` | marketplace、two-sided、matching platform、job board、撮合、招聘 |
| `nonprofit` | nonprofit、charity、NGO、公益、慈善 |
| `landing_page` | landing page、waitlist、coming soon、落地页、等待名单、预热页 |
| `creator` | creator、influencer、personal brand、portfolio、newsletter、blog、创作者、个人品牌、作品集、博主 |
| `agency` | agency、client dashboard、white-label、代运营、代理、客户管理 |
| `unknown` | 无法判断业务类型 |

### 8.6 `pain_points`

| **标签** | **标准** |
| --- | --- |
| `cold_start` | cold start、zero to one、从 0 到 1、冷启动 |
| `no_users` | no users、zero users、nobody uses、没人用、没有用户、注册少 |
| `low_traffic` | low traffic、no traffic、get traffic、曝光低、没流量、访问少、没人看到 |
| `low_conversion` | low conversion、conversion、转化低、转化率、留资、购买 |
| `unclear_positioning` | positioning、target audience、value proposition、定位、目标用户、卖点 |
| `content_ideation` | post ideas、content ideas、what to post、不知道发什么、选题、内容方向 |
| `platform_fit` | which platform、best platform、where should、哪个平台、平台选择、怎么发 |
| `creator_discovery` | influencer、creator discovery、KOL、KOC、达人、红人、博主 |
| `ad_performance` | ROAS、CPC、ad performance、budget、广告、投放、素材表现 |
| `retention_operation` | retention、newsletter、CRM、community、复购、社群、持续运营 |
| `not_clear` | 没有明确痛点信号 |

## 9. 二轮量化评分标准

### 9.1 紧迫度评分 `urgency_score`

基础分 0，最高 100。

| **加分项** | **分值** |
| --- | --- |
| 出现 asap、urgent、immediately、right now、today、马上、立即、尽快 | +35 |
| 出现 this week、soon、go live、准备上线、即将发布、预热、coming soon、waitlist | +25 |
| 出现 launch、launched、release、publish、上线、发布、首发、投放 | +20 |
| 出现 cold start、no users、get users、low traffic、low conversion、没人用、没流量、冷启动、找用户、转化低 | +30 |
| 后续用户消息继续围绕 marketing、content、caption、hashtag、launch、growth、traffic、campaign、营销、推广、社媒、文案、获客、引流等推进 | +10 |

分档：

| **分数** | **标签** |
| --- | --- |
| >= 60 | high |
| >= 30 且 < 60 | medium |
| >= 1 且 < 30 | low |
| 0 | unclear 或 low，视主意图而定 |

若 `context_primary_intent = platform_integration` 或 `context_relevance in (low, irrelevant)`，紧迫度直接记为 low。

### 9.2 需求强度评分 `demand_strength_score`

基础分 0，范围 0-100。

| **条件** | **分值** |
| --- | --- |
| 出现 App 推广、增长、内容生成相关动作 | +25 |
| `promoted_object = own_built_app` | +25 |
| `promoted_object = own_business` | +20 |
| `promoted_object in (client_business, social_account)` | +10 |
| 痛点包含 cold_start、no_users、low_traffic、low_conversion | +25 |
| 阶段为 pre_launch、launch、post_launch | +15 |
| 同时有平台和内容形式 | +10 |
| 仅有平台或内容形式之一 | +5 |
| 后续消息继续围绕营销/内容/增长推进 | +10 |
| 业务类型已知、目标用户/受众明确、价值/卖点/痛点明确，三项中满足至少两项 | +10 |
| 技术集成信号强，且推广对象不是 own_built_app、own_business、marketing_tool | -40 |
| 推广对象为 unknown 或 content_asset，且只有内容形式、没有增长或 App 推广动作 | -25 |

分档：

| **分数** | **标签** |
| --- | --- |
| >= 70 | strong |
| >= 40 且 < 70 | medium |
| >= 1 且 < 40 | weak |
| 0 | unclear |

### 9.3 Agent 适配度评分 `agent_fit_score`

基础分 0，范围 0-100。

| **条件** | **分值** |
| --- | --- |
| 业务类型已知，或推广对象不是 unknown/content_asset | +20 |
| 可映射到明确 `agent_opportunity` | +25 |
| 有明确 deliverable，如 caption、copy、script、plan、calendar、strategy、hashtag、title、post、文案、脚本、计划、日历、策略、标题、帖子 | +20 |
| 有明确平台 | +15 |
| 阶段为 pre_launch、launch、post_launch | +15 |
| 内容形式包含 content_calendar，或出现持续运营信号 | +10 |
| 主意图为 platform_integration | -40 |
| 需要竞品、最新趋势、实时数据、爬取等外部实时数据 | -20 |

该分数用于判断样本是否适合进入社媒营销 Agent 产品机会池，不等同于需求强度。

### 9.4 `agent_opportunity` 映射规则

| **标签** | **规则** |
| --- | --- |
| `channel_integration` | 主意图为 platform_integration |
| `marketing_tool_template` | 主意图为 marketing_tool_builder，或推广对象为 marketing_tool |
| `growth_diagnosis` | 痛点包含 cold_start、no_users、low_traffic、low_conversion |
| `short_video_script_pack` | 内容形式包含 short_video_script |
| `content_calendar` | 内容形式包含 content_calendar |
| `social_launch_pack` | 阶段为 pre_launch 或 launch，且推广对象为 own_built_app 或 own_business |
| `creator_branding` | 推广对象为 social_account 或 content_asset |
| `platform_specific_copy` | 有平台，且内容形式包含 caption、title、hashtag、image_post、thread、ad_copy |
| `not_applicable` | 不适合作为社媒营销 Agent 机会 |

### 9.5 `needs_more_context` 规则

满足任一条件则标记为 true：

1. `promoted_object = unknown` 且 `context_relevance in (high, medium)`。
2. `is_promoting_own_app_context = unclear` 且 `demand_strength_score >= 40`。
3. `confidence < 0.70`。

## 11. 质量控制与抽检标准

### 11.1 人工抽检建议

建议先做 300-500 条人工标注样本，覆盖以下维度：

1. HARD、SOFT、WEAK 命中层级。
2. 不同平台，如 Instagram、TikTok、YouTube、LinkedIn、小红书、抖音。
3. 不同长度段，如 `content_len <= 1000`、`1000-10000`、`10000-100000`。
4. 不同月份。
5. 不同主意图，如 app_promotion、content_generation、growth_strategy、marketing_tool_builder、platform_integration。

### 11.2 目标质检指标

| **指标** | **目标** |
| --- | --- |
| relevance precision | >= 85% |
| primary_intent 准确率 | >= 80% |
| platform 抽取准确率 | >= 90% |
| content_format 抽取准确率 | >= 85% |
| app_promotion vs marketing_tool_builder 区分准确率 | >= 80% |

### 11.3 规模校正建议

不要只看关键词命中数量。建议按分层 precision 校正真实需求规模：

```
校正后需求用户数 =
HARD 用户数 * HARD precision
+ SOFT 用户数 * SOFT precision
+ WEAK 用户数 * WEAK precision
```

同理可计算 prompt、chat 口径下的校正规模。

## 12. 主要风险与后续优化

### 12.1 当前规则风险

| **风险** | **影响** | **修正方式** |
| --- | --- | --- |
| 平台词误命中 | 登录、API、页脚链接、分享按钮被误判为营销需求 | 加强 integration/noise 排除，二轮上下文校正 |
| 单条 prompt 信息不足 | 无法判断是否推广自己构建的应用 | 补 chat 首条 prompt 和前后用户 prompt |
| 长文本和源码污染 | PRD、网页源码、日志里出现大量关键词 | 使用长度过滤和技术噪音规则 |
| marketing_tool_builder 混入 app growth | 把构建营销工具误当成用户推广自己 app | 用 promoted_object 和 segment 拆开 |
| unknown 过高 | 业务类型、推广对象仍不明确 | 补 app 元数据、发布状态、应用链接 |

### 12.2 后续推荐数据增强

优先补充以下元数据：

| **数据** | **字段示例** | **价值** |
| --- | --- | --- |
| app 元数据 | app name、description、created_at、published_at、app type、public URL | 判断是否真实自建应用、应用类型和上线阶段 |
| chat 元数据 | title、created_at、message_count、first hit turn | 判断会话主题和需求出现位置 |
| agent 最终回复 | final_response、turn_last_message | 判断 agent 是否已经给出营销方案或只是技术实现 |
| 完整 content | 按 message_id 小批量回补 | 研究复杂高价值 case |

## 13. 最终口径建议

对外讨论时建议同时使用三层口径：

| **口径** | **推荐用途** |
| --- | --- |
| 关键词命中池 | 判断潜在需求覆盖面，不直接代表真实需求 |
| 一轮 high/medium 核心池 | 判断社媒营销相关需求的保守规模和内容/平台分布 |
| 二轮 core_builder_growth_cases | 判断面向 Atoms builder 自身应用增长 Agent 的核心立项规模 |

最关键的产品判断口径是：

```
core_builder_growth_cases =
promoted_object = own_built_app
AND context_relevance in (high, medium)
AND demand_strength in (strong, medium)
```

该口径用于评估“Atoms builder 是否需要面向自己构建应用的社媒内容营销与增长引流 Agent”。